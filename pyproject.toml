[tool.uv.workspace]
members = [
    "packages/shared",
    "packages/operator_sdk",
    "services/agent",
    "services/mcp",
    "services/platform",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

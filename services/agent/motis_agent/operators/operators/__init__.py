# Operator modules live here.
# Each .py file (except those starting with _) is loaded automatically
# by the OperatorRegistry in dev mode.
#
# Contract: every module must export STATE, MANIFEST, build_graph().
# See: docs/operators/02-contract-and-validation.md
